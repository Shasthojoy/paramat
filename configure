echo "CC=clang" > Makefile.rules
echo 'SRC=mat_build.c main.c' >> Makefile.rules
echo 'OBJ=$(SRC:.c=.o)' >> Makefile.rules
echo "BIN=mul_test" >> Makefile.rules
echo "FLAGS=-Wall -Wextra -Werror -pedantic -std=c99" >> Makefile.rules

